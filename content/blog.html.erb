<div class="panel_title">
  <span class="slashes">// </span>
  <h1>Blog</h1>
</div>

<div class="panel_content">
 <% articles = @items.select{|item| item.attributes[:kind] == 'article'}.sort_by{|article| article.attributes[:created_at]}.reverse %>  <!-- todo: put in a helper -->
  <% articles.each do |article| %>
    <%= article.compiled_content(:snapshot => :last) %>
  <% end %>
</div>